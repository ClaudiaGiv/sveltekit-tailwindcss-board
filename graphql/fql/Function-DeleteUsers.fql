CreateFunction({
    name: "DeleteUsers",
    body:
        Query(
            Lambda([],
                Map(
                  Paginate(
                    Documents(Collection('User'))
                  ),
                  Lambda(
                    ['ref'],
                    Delete(Var('ref'))
                  )
                )
            )
        )
})
